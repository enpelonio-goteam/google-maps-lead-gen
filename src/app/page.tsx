export default function Home() {
  return (
    <main>
      <h1 style={{ marginTop: 0 }}>Google Maps Lead Gen API</h1>
      <p>This app exposes a single unauthenticated POST endpoint:</p>
      <pre style={{ background: "#f4f4f4", padding: 12, borderRadius: 8 }}>
        POST /api/leads
      </pre>
      <p>See <code>README.md</code> for request/response details.</p>
    </main>
  );
}


